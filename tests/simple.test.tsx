describe("Math tests", () => {
  it("adds two numbers correctly", () => {
    const sum = 2 + 3;
    expect(sum).toBe(5);
  });

  it("multiplies two numbers correctly", () => {
    const product = 4 * 5;
    expect(product).toBe(20);
  });
});
